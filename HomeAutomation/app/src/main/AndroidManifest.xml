<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.shashank.homeautomation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BeforeMain_1" />
        <activity android:name=".BeforeMain_2" />
        <activity android:name=".BeforeMain_3" />
        <activity android:name=".BeforeMain_2" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".BeforeMain_4" />
        <activity android:name=".ParticularRoomActivity"></activity>
    </application>

</manifest>